#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
int mod = 1000000007;

void solve() {
  int n, m, k;
  cin >> n >> m >> k;
  int l = (n-1) * (m-1);
  if (k < (n-1) * (m-1) || (n * m % k % 2 == 1)) {
    cout << "NO";
  } else {
    for (int i = 0; i < n; i++) {
      for (int j = 1; j < m; j++) {
        if ()
      }
    }
  }
}

int main() {
  // solution goes here
  int t; cin >> t;
}

